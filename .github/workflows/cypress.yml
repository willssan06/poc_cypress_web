name: Cypress Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v3

      - name: 🧪 Executa Cypress + Gera Relatório
        uses: cypress-io/github-action@v6
        with:
          build: npm install
          start: ''
          wait-on: ''
          command: npm run test:report

      - name: 📤 Publica relatório (artifact)
        uses: actions/upload-artifact@v3
        with:
          name: cypress-report
          path: cypress/reports/html
